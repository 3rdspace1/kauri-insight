# Database
DATABASE_URL=postgresql://user:password@localhost:5432/kauri_insight

# Auth
NEXTAUTH_URL=http://localhost:3000
NEXTAUTH_SECRET=your-secret-here-generate-with-openssl-rand-base64-32
NEXTAUTH_EMAIL_FROM=noreply@example.com

# AI (Optional - uses mock if not set)
MODELSLAB_API_KEY=

# Graphics (Optional - uses placeholders if not set)
NANO_BANANA_API_KEY=

# Integrations (Optional - logs to console if not set)
SLACK_WEBHOOK_URL=
RESEND_API_KEY=

# Email SMTP (Only if using Resend)
SMTP_HOST=smtp.resend.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=
